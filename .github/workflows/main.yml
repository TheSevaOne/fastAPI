name: App test
on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
jobs:
   startup:
      runs-on: ubuntu-latest
      steps:
          - uses: actions/checkout@v2
          - name: Run
          - run: pwd
        
